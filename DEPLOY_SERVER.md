# 🚀 Инструкция по деплою сервера на хостинг

Сервер для отправки сообщений в Telegram можно разместить на бесплатных платформах. Рекомендуем **Render.com** — простой и надежный вариант.

---

## 📋 Вариант 1: Render.com (Рекомендуется)

### Шаг 1: Регистрация
1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub (или email)

### Шаг 2: Создание нового сервиса
1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий (или загрузите файлы)
3. Настройки:
   - **Name:** `telegram-bot-server` (или любое имя)
   - **Environment:** `Node`
   - **Build Command:** (оставьте пустым)
   - **Start Command:** `node server.js`
   - **Plan:** `Free` (бесплатный)

### Шаг 3: Переменные окружения
В разделе **"Environment"** добавьте:
- `TELEGRAM_BOT_TOKEN` = `8169125582:AAHdwp0dqSn3_o2MB4EXdJzuWj4qifsrc3Y`
- `TELEGRAM_CHAT_ID` = `467035682`

### Шаг 4: Деплой
1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (2-3 минуты)
3. Скопируйте URL вашего сервера (например: `https://tgpfm.onrender.com`)

### Шаг 5: Обновление script.js
Замените в `script.js` строку:
```javascript
const SERVER_URL = 'http://localhost:3000';
```
на:
```javascript
const SERVER_URL = 'https://tgpfm.onrender.com';
```

---

## 📋 Вариант 2: Railway.app

### Шаг 1: Регистрация
1. Перейдите на https://railway.app
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создание проекта
1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий

### Шаг 3: Настройка
1. Railway автоматически определит Node.js
2. В настройках проекта добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN` = `8169125582:AAHdwp0dqSn3_o2MB4EXdJzuWj4qifsrc3Y`
   - `TELEGRAM_CHAT_ID` = `467035682`
3. Railway автоматически назначит URL (например: `https://ваш-проект.up.railway.app`)

### Шаг 4: Обновление script.js
Замените URL в `script.js` на URL от Railway.

---

## 📋 Вариант 3: Vercel (Serverless)

### Шаг 1: Установка Vercel CLI
```bash
npm i -g vercel
```

### Шаг 2: Создание vercel.json
Создайте файл `vercel.json`:
```json
{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/send-message",
      "dest": "server.js"
    }
  ]
}
```

### Шаг 3: Деплой
```bash
vercel
```

### Шаг 4: Переменные окружения
В настройках проекта Vercel добавьте переменные окружения.

---

## 📋 Вариант 4: Ваш собственный хостинг (cPanel/VPS)

Если у вас уже есть хостинг с поддержкой Node.js:

### Шаг 1: Загрузка файлов
Загрузите `server.js` и `package.json` на сервер через FTP/SSH

### Шаг 2: Установка зависимостей
```bash
npm install
```

### Шаг 3: Настройка переменных окружения
Создайте файл `.env`:
```
TELEGRAM_BOT_TOKEN=8169125582:AAHdwp0dqSn3_o2MB4EXdJzuWj4qifsrc3Y
TELEGRAM_CHAT_ID=467035682
PORT=3000
```

### Шаг 4: Запуск через PM2 (для постоянной работы)
```bash
npm install -g pm2
pm2 start server.js --name telegram-bot
pm2 save
pm2 startup
```

---

## 🔧 Обновление script.js для работы с хостингом

После деплоя сервера обновите `script.js`:

```javascript
// Замените эту строку:
const SERVER_URL = 'http://localhost:3000';

// На URL вашего сервера:
const SERVER_URL = 'https://tgpfm.onrender.com';
```

Затем используйте переменную в запросах:

```javascript
const response = await fetch(`${SERVER_URL}/send-message`, {
```

---

## ✅ Проверка работы

1. Откройте сайт в браузере
2. Заполните форму обратной связи
3. Отправьте заявку
4. Проверьте, что сообщение пришло в Telegram

---

## 🔒 Безопасность

⚠️ **Важно:** На хостинге используйте переменные окружения для токена и chat_id, не храните их в коде!

---

## 💡 Рекомендации

- **Для начала:** Используйте Render.com — самый простой вариант
- **Для продакшена:** Рассмотрите Railway или собственный VPS с PM2
- **Бесплатные лимиты:** Render.com может "засыпать" после 15 минут бездействия (первый запрос будет медленным)

---

## 🆘 Проблемы и решения

**Сервер не отвечает:**
- Проверьте, что сервер запущен на хостинге
- Проверьте URL в `script.js`
- Проверьте CORS настройки (должны быть `Access-Control-Allow-Origin: *`)

**Сообщения не приходят:**
- Проверьте переменные окружения на хостинге
- Проверьте логи сервера на хостинге
- Проверьте токен бота и chat_id
